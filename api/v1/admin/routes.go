package common

import (
	"github.com/gin-gonic/gin"
)

// ApplyRoutes applies router to the gin Engine
func ApplyRoutes(r *gin.RouterGroup) {
	// v1admin := r.Group("/admin")
	// {
	// 	health.ApplyRoutes(v1admin)
	// }
}
